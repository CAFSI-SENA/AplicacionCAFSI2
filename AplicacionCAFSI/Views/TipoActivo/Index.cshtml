@model IEnumerable<AplicacionCAFSI.Models.TipoActivo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreTipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EstadoID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreTipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EstadoID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoActivoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoActivoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoActivoID })
        </td>
    </tr>
}

</table>
